# adt-ls tool surface → moved

**This file is superseded by [`adt-ls-reference.md`](adt-ls-reference.md)** — the
single authoritative, live-verified capability map (URI model, the `getLsUri`
resolver, the full method/tool matrix, the object-type support boundary, the proven
create→edit→activate→test→delete lifecycle, and gotchas).

Add new adt-ls findings to `adt-ls-reference.md`, not here.
